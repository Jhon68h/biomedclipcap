services:
  biomedCLIPCAP:
    build: .
    image: biomedclipcap
    container_name: biomedCLIPCAP
    working_dir: /workspace
    volumes:
      - ./:/workspace
      - /home/Data/jefelitman_pupils/CLIP_prefix_caption/Sun_Multimodal:/data/jefelitman_pupils/Sun_Multimodal:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    stdin_open: true
    tty: true